#include<bits/stdc++.h>
using namespace std;

#define int long long
#define Mp make_pair
#define fi first
#define se second
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()

const int MOD = (int) 1e9 + 7;
const int inf = (int) 1e18 + 7;
using pii = pair<int,int>;

/// code
const int maxn = (int) 5e5 + 3;
pii f[maxn];
int n;

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    int d1 = 0, d2 = 0;
    cin >> n;
    for(int i = 1; i <= n; i++){
        cin >> f[i].fi >> f[i].se;
        d1 += f[i].fi;
        d2 += f[i].se;
    }

    if((d1 + d2) & 1){
        cout << -1;
        return 0;
    }

    for(int i = 1; i <= n && d1 % 2 != 0; i++){
        if(f[i].fi % 2 != f[i].se % 2){
            cout << 1;
            return 0;
        }
    }

    if(d1 % 2 == 0 && d2 % 2 == 0) cout << 0;
    else cout << -1;
    return 0;
}
///      Today is 02.09.2024
///    Do your best because tomorrow will be better!
///                                sign
///                               Justiin
