#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define Mp make_pair
#define fi first
#define se second
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()

const int MOD = (int) 1e9 + 7;
const ll inf = (ll) 1e18 + 7;
using pii = pair<int,int>;

/// code
const int maxn = (int) 1e5 + 3;
bool col[maxn], row[maxn];
int n, m, k;

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    cin >> n >> m >> k;

    while(k--){
        int a, b; cin >> a >> b;
        row[a] = 1;
        col[b] = 1;
    }

    int a = n, b = m;
    for(int i = 1; i <= n; i++) if(!row[i]) a--;
    for(int i = 1; i <= m; i++) if(!col[i]) b--;

    ll ans = 1ll * a * b;
    cout << ans;

    return 0;
}
///      Today is 21.09.2024
///    Do your best because tomorrow will be better!
///                                sign
///                               Justiin
