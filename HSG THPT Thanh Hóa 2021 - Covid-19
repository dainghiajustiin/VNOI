#include<bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    #define TASK "BAI1"
    freopen(TASK".inp", "r", stdin);
    freopen(TASK".out", "w", stdout);

    long long n, a, b;

    cin >> n >> a >> b;

    cout << max(a - b, 0LL) << ' ' << min(n - b, a);

    return 0;
}
