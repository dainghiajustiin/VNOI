#include<bits/stdc++.h>
using namespace std;

#define ll long long
#define Mp make_pair
#define fi first
#define se second
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()

const int MOD = (int) 1e9 + 7;
const ll inf = (ll) 1e18 + 7;
using pii = pair<int,int>;

/// code
ll get(ll l, ll r, int n){
    ll res = r/n - l/n;
    if(l%n == 0) res++;
    return res;
}

void solve(){
    ll l, r; cin >> l >> r;

    ll ans = get(l, r, 4) + get(l, r, 7) + get(l, r, 11) - get(l, r, 28)
                - get(l, r, 44) - get(l, r, 77) + get(l, r, 308);
    cout << ans << el;
}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    int t; cin >> t;
    while(t--) solve();

    return 0;
}
///      Today is 13.09.2024
///    Do your best because tomorrow will be better!
///                                sign
///                               Justiin
