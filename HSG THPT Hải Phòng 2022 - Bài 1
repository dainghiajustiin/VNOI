#include<bits/stdc++.h>
using namespace std;

string virus = "virus";
string s, slide;
int ans = 0;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

//    #define TASK ""
//    if(fopen(TASK".inp", "r")){
//        freopen(TASK".inp", "r", stdin);
//        freopen(TASK".out", "w", stdout);
//        freopen(TASK".ans", "w", stdout);
//    }

    cin >> s;
    if(s.size() < 5) return cout << 0, 0;

    slide = " ";
    for(int i = 0; i < 4; i++) slide += s[i];

    for(int i = 4; i < s.size(); i++){
        slide.erase(slide.begin());
        slide += s[i];
        if(slide == virus) ans++;
    }

    cout << ans;
    return 0;
}
