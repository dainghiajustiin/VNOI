#include<bits/stdc++.h>
using namespace std;

#define fi first
#define se second
#define ll long long
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()
#define Mp make_pair
#define lg2(x) 31 - __builtin_clz(x)
using pii = pair<int,int>;
const int MOD = (int) 1e9 + 7;
const ll inf = (ll) 1e18 + 7;
using ull = unsigned long long;

/// code
const int maxn = (int) 1e5 + 3;

int n;
ll b[maxn];
ll c[maxn];

signed main(){
    cin.tie(0)->ios_base::sync_with_stdio(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    if(fopen("buon.inp", "r")){
        freopen("buon.inp", "r", stdin);
        freopen("buon.out", "w", stdout);
    }

    cin >> n;

    for(int i = 1; i <= n; i++) cin >> b[i];
    for(int i = 1; i <= n; i++) cin >> c[i];

    sort(c + 1, c + 1 + n);

    ll ans = inf;

    c[0] = c[n + 1] = c[n + 2] = inf;

    for(int i = 1; i <= n; i++){
        int it = lower_bound(c + 1, c + 1 + n, -b[i]) - c;

        ans = min({ans, abs(b[i] + c[it]), abs(b[i] + c[it - 1]), abs(b[i] + c[it + 1])});
    }

    cout << ans;

    return 0;
}   /// Justiin
