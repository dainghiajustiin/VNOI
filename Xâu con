#include<bits/stdc++.h>
using namespace std;

#define fi first
#define se second
#define ll long long
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()
#define Mp make_pair
#define ld long double
using pii = pair<int,int>;
const int MOD = (int) 1e9 + 7;
const ll inf = (ll) 1e18 + 7;
using ull = unsigned long long;

/// code
const int maxn = (int) 1e6 + 3;
string s, t;
int base = 223;
const ll MM = 1ll*MOD*MOD;
int n, m;
ll p[maxn];
ll ha[maxn];
ll hb;

signed main(){
    cin.tie(0)->ios_base::sync_with_stdio(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    cin >> s >> t;

    int n = s.size(); s = ' ' + s;
    int m = t.size(); t = ' ' + t;

    p[0] = 1;
    for(int i = 1; i <= max(n, m); i++) p[i] = p[i - 1] * base % MOD;

    ha[0] = 0;
    for(int i = 1; i <= n; i++) ha[i] = (ha[i - 1]*base + s[i]) % MOD;

    hb = 0;
    for(int i = 1; i <= m; i++) hb = (hb*base + t[i]) % MOD;

    for(int i = 1; i + m - 1 <= n; i++){
        if((ha[i + m - 1] - ha[i - 1]*p[m] + MM) % MOD == hb) cout << i << ' ';
    }

    return 0;
}   /// Justiin
